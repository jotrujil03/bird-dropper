<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>About Bird Dropper</title>
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet" />
  <style>
    /* Reset & Base */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    html { font-size: 16px; }
    body {
      font-family: 'Roboto', sans-serif;
      color: var(--text);
      line-height: 1.6;
      background: #fff;
    }
    a { color: inherit; text-decoration: none; }

    /* CSS Variables */
    :root {
      --primary: #1a73e8;
      --primary-dark: #1669c1;
      --text: #202124;
      --text-light: #5f6368;
      --bg-light: #f8f9fa;
      --spacing: 1rem;
      --container-width: 1200px;

      /* New: global corner‑radius */
      --radius: 1rem;
    }

    /* Utility */
    .container {
      max-width: var(--container-width);
      margin: 0 auto;
      padding: 0 var(--spacing);
    }
    img {
      display: block;
      max-width: 100%;
      height: auto;
    }

    /* Skip‑Link */
    .skip-link {
      position: absolute;
      left: -9999px;
      width: 1px;
      height: 1px;
    }
    .skip-link:focus {
      left: var(--spacing);
      top: var(--spacing);
      background: var(--primary);
      color: #fff;
      padding: 0.5rem;
      border-radius: 0.25rem;
      z-index: 1000;
    }

    /* ——— Hero Section as Card ——— */
    .hero {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      padding: 6rem var(--spacing);
      background: linear-gradient(135deg,
                  rgba(26,115,232,0.1) 0%,
                  rgba(26,115,232,0) 100%);

      /* ← new card styling */
      border-radius: var(--radius);
      box-shadow: 0 6px 20px rgba(0,0,0,0.05);
      overflow: hidden;
      margin: 2rem 0;
    }
    .hero__content {
      flex: 1;
      min-width: 300px;
      opacity: 0;
      transform: translateX(-20px);
      animation: slideInLeft 0.8s ease-out forwards;
    }
    .hero__content h1 {
      font-size: 2.75rem;
      margin-bottom: var(--spacing);
      font-weight: 700;
    }
    .hero__content p {
      font-size: 1.25rem;
      margin-bottom: calc(var(--spacing) * 1.5);
      color: var(--text-light);
    }
    .btn {
      display: inline-block;
      background: var(--primary);
      color: #fff;
      padding: 0.75rem 1.5rem;
      border-radius: var(--radius);
      box-shadow: 0 4px 12px rgba(26,115,232,0.3);
      transition: background 0.3s, transform 0.2s;
    }
    .btn:hover {
      background: var(--primary-dark);
      transform: translateY(-2px);
    }
    .hero__image {
      flex: 1;
      min-width: 300px;
      text-align: center;
      opacity: 0;
      transform: translateX(20px);
      animation: slideInRight 0.8s ease-out forwards;
    }
    .hero__image img {
      border-radius: var(--radius);
    }

    /* Info Cards */
    .info {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 2rem;
      margin: 2rem 0;
      background: #fff;
      border-radius: var(--radius);
      overflow: hidden;
      box-shadow: 0 6px 20px rgba(0,0,0,0.05);
      transition: transform 0.3s;
    }
    .info:hover { transform: translateY(-5px); }
    .info--reverse { flex-direction: row-reverse; }
    .info__img { flex: 1; }
    .info__body {
      flex: 1;
      padding: 2rem;
    }
    .info__body h2 {
      color: var(--primary);
      margin-bottom: 0.5rem;
    }
    .info__body p {
      color: var(--text-light);
    }

    /* Stats Panels */
    .stats {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-around;
      text-align: center;
      padding: 4rem 0;
      background: var(--bg-light);
    }
    .stats__item {
      flex: 1;
      min-width: 200px;
      padding: 1.5rem;
      background: #fff;
      border-radius: var(--radius);
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      margin: 0.5rem;
    }
    .stats__number {
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 0.5rem;
    }

    /* Values Grid */
    .values {
      text-align: center;
      padding: 4rem 0;
    }
    .values h2 {
      color: var(--primary);
      font-size: 2.5rem;
      margin-bottom: 1.5rem;
    }
    .values__grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 2rem;
    }
    .value {
      background: #fff;
      padding: 2rem;
      border-radius: var(--radius);
      box-shadow: 0 4px 16px rgba(0,0,0,0.04);
    }
    .value h3 {
      color: var(--primary);
      margin-bottom: 0.5rem;
    }
    .value p { color: var(--text-light); }

    /* Animations */
    @keyframes slideInLeft {
      to { opacity: 1; transform: translateX(0); }
    }
    @keyframes slideInRight {
      to { opacity: 1; transform: translateX(0); }
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero { flex-direction: column; text-align: center; }
      .info { flex-direction: column; }
    }
  </style>
</head>
<body>
  <a href="#main-content" class="skip-link">Skip to content</a>
  <!-- your nav here -->
  <main id="main-content">
    <!-- Hero as Card -->
    <section class="hero container">
      <div class="hero__content">
        <h1>About Bird Dropper</h1>
        <p>Making bird watching accessible with innovative tracking solutions.</p>
        <a
        href="https://www.youtube.com/watch?v=DSIhFy6tlvI&t=19s"
        class="btn"
        target="_blank"
        rel="noopener noreferrer"
        >
        Get Started
        </a>
      </div>
      <div class="hero__image">
        <!-- Make sure this file exists at /uploads/hero-birds.jpg -->
        <img src="/uploads/hero-birds.png" alt="Flock of birds" loading="lazy" />
      </div>
    </section>

    <!-- Our Story / Approach / Team -->
    <section class="container">
      <div class="info">
        <div class="info__img">
          <img src="/uploads/ourStory.jpg" alt="Our Story" loading="lazy" />
        </div>
        <div class="info__body">
          <h2>Our Story</h2>
          <p>Bird Dropper began with a simple idea: helping bird enthusiasts track and identify their feathered friends. Our community to bird watchers worldwide.</p>
        </div>
      </div>

      <div class="info info--reverse">
        <div class="info__img">
          <img src="/uploads/ourApproach.jpg" alt="Our Approach" loading="lazy" />
        </div>
        <div class="info__body">
          <h2>Our Approach</h2>
          <p>We believe technology should enhance our connection to nature. Our tools are intuitive, unobtrusive, and educational.</p>
        </div>
      </div>

      <div class="info">
        <div class="info__img">
          <img src="/uploads/ourTeam.jpg" alt="Our Team" loading="lazy" />
        </div>
        <div class="info__body">
          <h2>Our Team</h2>
          <p>A diverse group of ornithologists, developers, and designers passionate about birds. We’re building the world’s most comprehensive bird tracking platform.</p>
        </div>
      </div>
    </section>


    <!-- Values -->
    <section class="values container">
      <h2>Our Values</h2>
      <div class="values__grid">
        <div class="value">
          <h3>Innovation</h3>
          <p>Continuously improving our technology to better serve bird enthusiasts.</p>
        </div>
        <div class="value">
          <h3>Education</h3>
          <p>Sharing knowledge about birds and their ecosystems.</p>
        </div>
        <div class="value">
          <h3>Conservation</h3>
          <p>Supporting efforts to protect bird habitats around the world.</p>
        </div>
        <div class="value">
          <h3>Community</h3>
          <p>Fostering connections among bird watchers globally.</p>
        </div>
      </div>
    </section>
  </main>

  <script>
    // Animate stats on scroll
    document.addEventListener('DOMContentLoaded', () => {
      const stats = document.querySelectorAll('.stats__number');
      const animate = el => {
        const target = +el.dataset.target, step = target / 200;
        let count = 0;
        (function update() {
          count < target
            ? (el.textContent = Math.ceil(count += step), setTimeout(update, 10))
            : (el.textContent = target);
        })();
      };
      stats.forEach(s => new IntersectionObserver(
        entries => entries.forEach(e => e.isIntersecting && animate(e.target)),
        { threshold: 0.5 }
      ).observe(s));
    });
  </script>
</body>
</html>
