<!DOCTYPE html>
<html lang="{{language}}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Bird Dropper - Search Results for "{{query}}"</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    /* ... (Your existing styles) ... */
    .bird-info-container {
      margin-top: 20px;
      border: 1px solid #ccc;
      padding: 15px;
      border-radius: 5px;
    }

    .bird-image {
      max-width: 300px;
      height: auto;
      margin-bottom: 10px;
    }

    .image-gallery {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-bottom: 10px;
    }

    .image-gallery .bird-image {
      max-width: 150px; /* Adjust as needed */
      height: auto;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">Search Results for "{{query}}"</h1>

    {{#if error}}
      <div class="alert alert-danger">{{error}}</div>
    {{else}}
      {{#if results.length}}
        <div class="search-results-container">
          {{#each results}}
            <div class="card mb-3">
              <div class="card-body">
                {{#if this.images.length}}
                  <div class="image-gallery">
                    {{#each this.images}}
                      <img src="{{this}}" alt="{{../this.name}}" class="bird-image">
                    {{/each}}
                  </div>
                {{else if this.image}}
                  <img src="{{this.image}}" alt="{{this.name}}" class="bird-image">
                {{/if}}
                <h3>{{this.name}}</h3>
                {{#if this.info}}
                  <p>{{this.info}}</p>
                {{else}}
                  <p>No information found on Wikipedia for "{{query}}".</p>
                {{/if}}
              </div>
            </div>
          {{/each}}
        </div>
      {{else}}
        <p>No results found for "{{query}}".</p>
      {{/if}}
    {{/if}}

    <div class="mt-4">
      <a href="/" class="btn btn-secondary">Back to Home</a>
    </div>
  </div>

  {{> footer}}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>